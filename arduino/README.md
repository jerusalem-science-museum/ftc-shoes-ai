# arduino code

one of these .ino files (not sure which, when you know update this) are run on the arduino that is connected to the language button, and when press the arduino simulates a keyboard language press (`left_alt+left_shift`). so even though the alt+shift keys are removed physically in the keyboard we can still change langauges using the big button.